# LoopDeck

LoopDeck 是基于 One Tool（2022）二次开发的一套开源云任务系统，支持网易云音乐、哔哩哔哩等平台的等级与日常任务。

项目已移除原作者失效的旧协议与加密文件，并对任务调度、平台协议、容器部署、日志和更新流程进行了持续重构。

## 当前版本

### v1.2.1（2026-09-06）

**邮件依赖维护与通知资源优化**

- 邮件统一使用 Composer 锁定的 PHPMailer 7.1.1，移除旧版手工拷贝；同一批通知复用 SMTP 连接，每封邮件独立清理收件人，失败、凭据变化或批次结束时释放连接。
- HTTP 推送客户端按需创建，未开启的通知提前返回；版本号在进程内复用，减少重复加载和文件读取。
- 单个用户的每日总览异常不再阻塞其他通知；邮件问候语只提取昵称，避免把用户对象中的私有字段放入邮件。
- 保持 PHP 8.1+ 和现有容器兼容，无新增常驻服务、数据库迁移或环境变量。选型理由、对照基准和部署步骤见 [依赖优化验证记录](docs/DEPENDENCY-OPTIMIZATION.md)。

## 网易云每日300首说明

**网易云每日300首：终身未听硬过滤与有限自适应补批**

- 网易云协议与 [NeteaseCloudMusicApiEnhanced/api-enhanced](https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced) 对齐：每日任务使用 `/api/feedback/weblog` 的 `startplay`/`play` 两阶段即时上报；首次选歌使用 `/api/v1/play/record` 的 `allData`（全部记录）和 `weekData`（近一周记录）初始化历史。
- 候选歌曲只接受账号历史中没有出现过的歌曲；历史记录以及本项目成功上报的歌曲保存到 `runtime/netease-daka/` 的账号哈希文件中，状态写入采用原子替换，不再有 30,000 首截断。接口返回的历史会在首次运行时加入硬过滤，之后每次成功上报继续追加。
- 选歌顺序调整为“自定义/个性化歌单 → 多轮长尾搜索歌单 → 官方榜单兜底”。搜索只负责发现歌单 ID，歌曲仍从 `playlist_detail` 获取并携带真实 `sourceId`；歌曲不足 60 秒时仅在候选不足的第二轮放宽到 30 秒。
- 上报后先短暂核验（默认 15 秒），未入账时保留当天任务并默认每 5 分钟自动复查；前一批等待 3 个核验间隔后仍有差额，才使用未提交过的新歌补齐。已即时入账的差额也可在本次调用内补齐，核验期间不会持续占用 PHP worker。
- 每天默认最多上报 6 批，批次额度在进程重启和后续调用之间共享；额度用完后仍会核验已上报歌曲，避免晚到账被误报失败。当天达到 300 首后停止上报和重试；当天结束仍未达到目标才记录最终失败。
- 网络请求前先保存基线、批次和歌曲 ID，进程中断后继续按原始基线核验，避免重复上报。读取不到有效累计数或无法保存进度时暂不上报并等待重试。
- 首批会在目标之外预留有限替换歌曲（默认目标的 20%，上限 60 首），但最终完成判断仍只看网易云累计 `listenSongs`；第三方接口限流、账号状态或异步结算异常时不能承诺绝对入账。
- 公共歌单曲目列表按歌单和曲目数量使用 6 小时共享缓存，多账号不会重复下载相同歌单；账号历史仍按账号独立过滤，不把大历史集合写入数据库查询。

常用高级配置（均会在服务端校验范围）：

| 配置项 | 默认值 | 范围 | 作用 |
| --- | ---: | ---: | --- |
| `daka_search_rounds` | `4` | 1-8 | 长尾搜索轮数 |
| `daka_search_playlists_per_round` | `12` | 1-50 | 每轮最多读取的歌单数 |
| `daka_topup_batches` | `3` | 0-10 | 旧版兼容字段，不再控制补批 |
| `daka_max_batches_per_day` | `6` | 1-30 | 每天总上报批次上限，达到后仍核验已上报歌曲 |
| `daka_max_verification_runs` | `3` | 1-10 | 前一批入账等待窗口包含的核验间隔数 |
| `daka_retry_seconds` | `300` | 120-3600 | 自动核验间隔；上报额度用完后至少间隔 15 分钟 |
| `daka_internal_wait_seconds` | `15` | 0-60 | 返回调度器前短暂等待累计听歌结算的秒数 |
| `daka_min_song_seconds` | `60` | 30-600 | 首轮歌曲时长下限 |

大历史账号的性能说明：历史文件按账号 SHA-256 分片，读取和过滤只发生在当前任务进程内；首次初始化只调用两次播放记录接口，后续不重复初始化。歌单详情使用进程内及共享缓存，账号删除时会同时清理对应状态文件；定时清理只匹配 LoopDeck 自己生成的状态文件，不会误删运行目录中的其他文件。
