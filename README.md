# LoopDeck

LoopDeck 是基于 One Tool（2022）二次开发的一套开源云任务系统，支持网易云音乐、哔哩哔哩等平台的等级与日常任务。

项目已移除原作者失效的旧协议与加密文件，并对任务调度、平台协议、容器部署、日志和更新流程进行了持续重构。

## 当前版本

### v1.2.0（2026-09-05）

**个人推送中心、每日总览与调度修复**

- 个人中心统一配置 Bark、PushPlus、WxPusher 和邮件，分别选择任务成功、最终失败、账号失效、会员到期、每日总览与 Epic 周免提醒。Bark 固定使用官方服务；凭据留空保留，保存后可测试渠道。
- 邮件仅在管理员开启且完整配置 SMTP 后显示和发送；关闭后接口也拒绝绕过，SMTP 密码和推送凭据不再回填到页面。
- 每日总览默认 22:00，按账号列出当天发送前各项任务的成功、失败和重试结果。投递在独立队列中执行，按事件和渠道去重，失败最多尝试三次，不重复运行已完成的任务。
- Epic 周免与小黑盒签到接入统一调度；保留旧 Bark 与已开启的 Epic 邮件订阅，Epic 支持所选推送渠道，修复周末计算多跳一周的问题。Epic 功能提供活动提醒，游戏仍需前往商店自行领取。
- 网易云保留每天 3–15 分钟偏移，同一日期计划保持稳定，页面显示包含日期的下次计划；修复哔哩哔哩偏移每天固定的问题。偏移用于分散执行时间，不能保证规避平台限制。
- 网易云每日300首保存当天基线与已提交歌曲，延迟入账显示“重试中”并自动核验，只有实际增量达到目标才显示成功；核验等待不再长时间占用 PHP worker。
- 加固整个调度应用的鉴权和路由白名单，阻止 `_ajax` 请求参数绕过来源校验，封禁账号同时停止任务；修复数据表格输出、会话来源、重复数据库查询及四字节字符存储问题。

升级涉及通知数据表与调度容器配置，按 [Docker 部署说明](DOCKER.md) 更新宿主机代码和三个应用服务；保留原有数据库和账号状态。

## 网易云每日300首说明

**网易云每日300首：终身未听硬过滤与有限自适应补批**

- 网易云协议与 [NeteaseCloudMusicApiEnhanced/api-enhanced](https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced) 对齐：每日任务使用 `/api/feedback/weblog` 的 `startplay`/`play` 两阶段即时上报；首次选歌使用 `/api/v1/play/record` 的 `allData`（全部记录）和 `weekData`（近一周记录）初始化历史。
- 候选歌曲只接受账号历史中没有出现过的歌曲；历史记录以及本项目成功上报的歌曲保存到 `runtime/netease-daka/` 的账号哈希文件中，状态写入采用原子替换，不再有 30,000 首截断。接口返回的历史会在首次运行时加入硬过滤，之后每次成功上报继续追加。
- 选歌顺序调整为“自定义/个性化歌单 → 多轮长尾搜索歌单 → 官方榜单兜底”。搜索只负责发现歌单 ID，歌曲仍从 `playlist_detail` 获取并携带真实 `sourceId`；歌曲不足 60 秒时仅在候选不足的第二轮放宽到 30 秒。
- 上报后先短暂核验（默认 15 秒），未入账时保留当天任务并默认每 5 分钟自动复查；前一批等待 3 个核验间隔后仍有差额，才使用未提交过的新歌补齐。已即时入账的差额也可在本次调用内补齐，核验期间不会持续占用 PHP worker。
- 每天默认最多上报 6 批，批次额度在进程重启和后续调用之间共享；额度用完后仍会核验已上报歌曲，避免晚到账被误报失败。当天达到 300 首后停止上报和重试；当天结束仍未达到目标才记录最终失败。
- 网络请求前先保存基线、批次和歌曲 ID，进程中断后继续按原始基线核验，避免重复上报。读取不到有效累计数或无法保存进度时暂不上报并等待重试。
- 首批会在目标之外预留有限替换歌曲（默认目标的 20%，上限 60 首），但最终完成判断仍只看网易云累计 `listenSongs`；第三方接口限流、账号状态或异步结算异常时不能承诺绝对入账。
- 公共歌单曲目列表按歌单和曲目数量使用 6 小时共享缓存，多账号不会重复下载相同歌单；账号历史仍按账号独立过滤，不把大历史集合写入数据库查询。

常用高级配置（均会在服务端校验范围）：

| 配置项 | 默认值 | 范围 | 作用 |
| --- | ---: | ---: | --- |
| `daka_search_rounds` | `4` | 1-8 | 长尾搜索轮数 |
| `daka_search_playlists_per_round` | `12` | 1-50 | 每轮最多读取的歌单数 |
| `daka_topup_batches` | `3` | 0-10 | 旧版兼容字段，不再控制补批 |
| `daka_max_batches_per_day` | `6` | 1-30 | 每天总上报批次上限，达到后仍核验已上报歌曲 |
| `daka_max_verification_runs` | `3` | 1-10 | 前一批入账等待窗口包含的核验间隔数 |
| `daka_retry_seconds` | `300` | 120-3600 | 自动核验间隔；上报额度用完后至少间隔 15 分钟 |
| `daka_internal_wait_seconds` | `15` | 0-60 | 返回调度器前短暂等待累计听歌结算的秒数 |
| `daka_min_song_seconds` | `60` | 30-600 | 首轮歌曲时长下限 |

大历史账号的性能说明：历史文件按账号 SHA-256 分片，读取和过滤只发生在当前任务进程内；首次初始化只调用两次播放记录接口，后续不重复初始化。歌单详情使用进程内及共享缓存，账号删除时会同时清理对应状态文件；定时清理只匹配 LoopDeck 自己生成的状态文件，不会误删运行目录中的其他文件。

## 历史更新

- `2026-09-04` `v1.1.28` 修复批量 EAPI 的 `NMTID` 握手时序，并让每日300首首批实际使用有限替换余量，覆盖 296/300 的计数短缺场景。

- `2026-09-04` `v1.1.27` 对齐 api-enhanced 服务端 `NMTID` 下发握手，避免预生成随机值导致听歌记录不入账。

- `2026-09-04` `v1.1.26` 网易云每日300首补齐与核验改为单次内部处理，最终只写一条成功/失败日志；兼容旧状态并修复 296/300 后停止的问题。

- `2026-09-04` `v1.1.25` 哔哩哔哩每日分享功能下架；移除每日经验中的分享请求；新增多源、带版本校验和自动回滚的定时容器更新器；后台改为只读更新状态。

- `2026-09-04` `v1.1.24` 对齐 api-enhanced 播放记录协议；修复历史种子字段和 uid；终身未听硬过滤；增加长尾歌单搜索、有限自适应补批、原子状态写入与大历史缓存；修复旧版调度器异常后错误推进任务的问题。
- `2026-09-01` `v1.1.23` 网易云每日300首消除重复上报：落账后当天仅核验不再重报，候选池新增搜索歌单深度新歌层，单批一次带满目标。
- `2026-08-12` `v1.1.22` 修复网易云每日300首跑不满；修复支付回调重放提权、后台跨分站越权与找回密码 Host 头注入等安全问题；优化候选采集性能。
- `2026-08-07` `v1.1.21` 恢复 Epic 周免邮箱订阅接口，移除侧边栏抖音开发中占位项。
- `2026-08-05` `v1.1.20` 新增抖音登录预览与账号列表，登录签名改为异步等待。
- `2026-08-03` `v1.1.19` 网易云每日300首改为按“今日已提交事件数”严格控额并异步复核补齐；优先新歌、放宽补齐批次上限至 20 次。（该控额方式会导致跑不满，已在 v1.1.22 改为按实测增量控额）
- `2026-08-02` `v1.1.18` 网易云账号密码添加组件恢复显示并标记维护中，移除“账号密码登录已关闭”提示。
- `2026-08-02` `v1.1.17` 网易云账号添加改为仅支持 APP 扫码登录，关闭账号密码表单与接口。
- `2026-08-02` `v1.1.16` 网易云每日补齐批次上限调整为3次，达到上限后停止当天重试。
- `2026-08-02` `v1.1.15` 网易云即时 scrobble 改为与 api-enhanced 完全一致的单日志请求，并通过受限并发保持快速执行。
- `2026-08-02` `v1.1.14` 网易云每日300首改为即时 scrobble，并以实际累计增量异步复核补齐。
- `2026-08-02 14:16:00` `v1.1.13` 修复网易云每日300首真实计数，精简任务响应并增加官方听歌记录去重。
- `2026-08-02 13:33:52` `v1.1.12` 精简网易云每日听歌任务说明。
- `2026-08-02 12:57:17` `v1.1.11` 使用每日推荐歌曲与歌单补足正常300首目标。
- `2026-08-02 12:46:00` `v1.1.10` 网易云听歌任务默认改用账号每日推荐内容。
- `2026-08-02 11:11:14` `v1.1.8` 增加同日重复执行保护，避免重复提交每日听歌任务。
- `2026-08-02 10:27:24` `v1.1.7` 新增 NCBL 客户端日志协议、加密实现和协议测试。
- `2026-08-02 09:37:47` `v1.1.6` 修复计划任务重复执行问题。
- `2026-08-02 09:12:21` `v1.1.5` 修复网易云听歌上报与多账号任务隔离。
- `2026-08-02 07:25:45` `v1.1.4` 新增网易云听歌工具与 Bark 账号失效通知。
- `2026-08-02 06:09:26` `v1.1.3` 更新哔哩哔哩登录及任务协议。
- `2026-08-01 22:49:36` `v1.1.2` 恢复二维码登录生成流程。
- `2026-08-01 22:28:34` `v1.1.1` 恢复后台模板设置。
- `2026-08-01 21:32:20` `v1.1.0` 加固依赖、任务日志和运行安全。
- `2026-08-01 00:36:19` 增加 Docker 部署、GHCR 镜像发布和后续自动更新能力。
- `2026-07-31 18:38:18` 修复统一任务调度器，恢复多平台云任务执行。
- `2026-07-31 16:54:41` 项目正式更名为 LoopDeck。
- `2026-07-31 16:19:40` 删除原作者失效旧协议、被加密的原核心文件和已退役功能，开始重构核心服务。
